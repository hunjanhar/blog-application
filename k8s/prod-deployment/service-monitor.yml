apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: blog-app-monitor
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app: blog-app
  namespaceSelector:
    matchNames:
      - blog-app-ns
  endpoints:
  - port: metrics
    path: /metrics
